service: divera-slack-app
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: eu-central-1

functions:
  test:
    handler: src/lambdas/test.handler
    url: true

plugins:
  - serverless-plugin-typescript
  - serverless-offline
